# v0.42.8 tokenizer calibration — notes draft

**Not ready to publish:** runtime integrations have landed on the task branch, but the complete Phase A inventory and verification matrix are not finished. See `PARITY.md` and `docs/reports/tokenizer-calibration-phase-a-verification.md`.

## Changes implemented

- History materialization converts a provider-token history allowance to a local prose allowance in OpenCode, Pi and Rust. Curve pressure and exact demotion use the same units. Conversion occurs when rendering, not in cache-invalidation identity.
- Protected tool-window membership uses class-calibrated mass, retaining whole tag groups and the newest-three structural minimum. Original source boundaries weight prose and tool input/output without changing stored raw counts.
- Raw fallback and last-known-good replay require complete calibrated estimates and a known current-model window. A byte-size approximation can reject early but cannot prove fit. Recovery cannot disarm on incomplete or nonfinite telemetry.
- Historian request admission counts the assembled source and system prompt under the selected producer's seed, reserves output once, and retains the 3% margin. Source chunk allowances use the producer model, not the main model.
- Emergency planning separates current served mass from retained replacement mass. Skeleton tokens are not credited as removed; cached content is previewed without mutation. The existing once-per-pressure-episode latch remains.
- Priced OpenCode passes, including Rust-hosted responses, capture raw returned-array measurements. A fresh system hook and correlated completed usage can update an in-process candidate EMA. **Candidate learning never drives decisions, including at N=3.** Pi and hostless Rust explicitly report partial observations where system/tools or response correlation are unavailable.

## Intentional behavior changes

**Fit guards can refuse requests they previously admitted.** The previous 1.02× oversized atomic-source fixture is one explicit example: clipping source text to a raw allowance does not prove the complete calibrated prompt fits. That case now sends no producer prompt, publishes no new range, and records `producer_prompt_exceeds_window` rather than advancing coverage. In particular, unknown models use `max(2.0, largest static measured ratio)` for fit, rather than trusting raw local counts. A family-inherited seed is treated as a seed, not as unknown. Missing measurements cannot be made complete by multiplying known components. Pi's storage-failure fallback currently lacks complete observations and therefore refuses rather than sending on a byte-only estimate. Rust refuses a fallback producer whose own context window has not been supplied; it does not borrow the primary producer's window.

`protected_tokens` remains a real/provider-token floor, and `history_budget_percentage` remains a fraction of the established real window. Existing configuration values are not rewritten or rescaled. In the integrated history/protection paths, calibration makes those values mean their documented units rather than raw tokenizer units. This can shorten history or protect fewer tool tags for models whose local tokenizer undercounts.

No new configuration knob, migration, schema fence change, sidecar, or persisted learning record is introduced. Tokenizer goldens and durable raw token counts are unchanged. The tool-schema ratio used for tool I/O is a policy seed, not a measurement proving every tool output has that drift. Provider framing and later host rewrites remain unmeasured.

## Remaining release work

- Tail-hygiene T/U and cadence/grace watermark unit transition is deferred pending a safe durable epoch/unit contract.
- Legacy historian projected-reclaim byte-ratio accounting remains to be replaced with served before/after accounting.
- The full m0 docs/memory/profile/facts and m1 cap inventory, including explicit raw/real status reporting, is not complete.
- The existing four-defer wire differential passed, but the strengthened seeded-generation/restart/table-revision differential, neutral-versus-Fable priced wire comparison and tail-only-bust comparison remain outstanding. The full executing adversarial transport matrix and defer-freeze mutation are not claimed complete.

## Fable 5.1 fixture impact

These are independently calculated allowance/reach estimates using the static seed, not a reconstruction of the private incident or a fresh provider measurement. History uses prose `1.571778`; tool protection uses the conservative tool-I/O policy seed `1.551639`. Rendered bodies may be smaller because tiers and whole arcs are discrete.

For a configured 60,000-provider-token history body allowance, the old 60,000-local allowance represented approximately 94,307 provider tokens. The new allowance is 38,173 local tokens, approximately 59,999.481594 provider tokens, within the unchanged 60,000-provider-token cap. For a 6,000-provider-token protected floor over eight 1,000-local-token tool tags, membership changes from tags 3–8 to tags 5–8; the crossing tag remains whole.

Fable 5.1 fixture: history allowance 94,307 → at most 60,000 estimated real tokens (38,173 local); protected reach 6 tags / 9,310 → 4 tags / 6,207 estimated real tokens.
