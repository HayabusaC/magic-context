{
  "gate": "Pi failed-group follow-up",
  "full_suite_record": "full-pi.json",
  "full_suite_rerun": false,
  "reason": "Only impacted groups were rerun, per the instruction not to repeat recorded gates.",
  "checks": [
    {
      "command": "bun test src/context-handler.test.ts src/context-handler-lkg.test.ts src/pi-lkg.test.ts src/issue-423.test.ts src/issue-424.test.ts src/index-in-process-latch.test.ts src/commands/ctx-commands.test.ts src/commands/ctx-wrapup.test.ts src/subagent-runner.test.ts src/tools/ctx-note-smart.test.ts src/pi-raw-fallback.test.ts",
      "working_directory": "packages/pi-plugin",
      "isolation": "empty inherited environment except PATH; existing throwaway HOME and XDG directories",
      "passed": 340,
      "failed": 4,
      "expect_calls": 76811,
      "remaining_failures": [
        "registerPiContextHandler > fires a recovery historian on the first pass after persisted failure",
        "registerPiContextHandler > keeps durable deferred publication signals when an in-flight historian publishes after session clear",
        "registerPiContextHandler > keeps deferred publication signals when an in-flight historian publishes for an active session",
        "registerPiContextHandler > isolates deferred publication signals across multiple in-flight sessions when one is cleared"
      ]
    },
    {
      "command": "bun run typecheck && bun test src/context-handler.test.ts --test-name-pattern 'fires a recovery historian|keeps durable deferred publication|keeps deferred publication signals|isolates deferred publication'",
      "working_directory": "packages/pi-plugin",
      "typecheck": "passed",
      "passed": 4,
      "failed": 0,
      "expect_calls": 12
    }
  ],
  "changes": [
    "Mock historian transports explicitly declare their model windows, including nested historian options; no guard was bypassed.",
    "The test HOME directory now exists before OMP helpers create children beneath it; the environment-only failures passed unchanged.",
    "Incomplete Pi raw/LKG admission now intentionally refuses even a small replay. Tests still prove durable hydration and SOFT+ capture while checking host refusal, rather than asserting a byte-only send.",
    "The pure mapped-contraction control supplies explicit closed-fixture system/tool observations and proves a fitting calibrated replay is accepted.",
    "Oversized byte-proxy rejection now occurs before expensive exact tokenization; admission still requires complete calibrated counts.",
    "The episode-latch fixture uses word-delimited token mass so its first calibrated batch leaves candidates for the rearm assertions."
  ],
  "result": "Every failure in the recorded full Pi run was covered by a passing targeted follow-up; no claim of a second full-suite run is made."
}
