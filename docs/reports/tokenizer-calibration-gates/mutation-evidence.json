{
  "recording": "Earlier executed NON-VACUITY BREAK runs, preserved here without rerunning recorded gates. Every mutation was staged against the live implementation before editing, showed a non-empty unstaged diff while applied, and was restored with git checkout -- <path> && touch <path>; the post-restore unstaged diff was empty.",
  "mutation_evidence": [
    {
      "control": "Replace the unknown-model provider-mass fit multiplier with 1.",
      "expected_red": "unknown calibrated raw fallback refuses a locally fitting request and admits a safe request",
      "captured_output": "FAIL unknown calibrated raw fallback refuses a locally fitting request and admits a safe request: expected rejection, promise resolved. PASS preserves raw fail-open when the estimate fits the known context limit. Only the named negative test failed.",
      "applied_evidence": "packages/plugin/src/hooks/magic-context/decision-calibration.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Ignore trusted=false in the actual Rust-mode adapter raw-fallback admission branch.",
      "expected_red": "refuses a tiny untrusted fallback estimate instead of treating the byte proxy as fit proof",
      "captured_output": "FAIL refuses a tiny untrusted fallback estimate instead of treating the byte proxy as fit proof: expected rejection, promise resolved. PASS preserves raw fail-open when the estimate fits the known context limit. Only the named negative test failed.",
      "applied_evidence": "packages/plugin/src/hooks/magic-context/rust-mode-transform.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Ignore the whole-estimate trusted flag in LKG-plus-suffix admission.",
      "expected_red": "refuses LKG plus suffix when tool-definition completeness is lost",
      "captured_output": "FAIL refuses LKG plus suffix when tool-definition completeness is lost: expected rejection, promise resolved. PASS replays the durably persisted snapshot after a simulated process restart. Only the named negative test failed.",
      "applied_evidence": "packages/plugin/src/hooks/magic-context/rust-mode-transform.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Permit an untrusted final-wire estimate to disarm provider-proven emergency recovery.",
      "expected_red": "emergency fail-closed decision > keeps provider-overflow blocking when the final-wire estimate is untrusted",
      "captured_output": "FAIL keeps provider-overflow blocking when the final-wire estimate is untrusted: unexpected trusted-final-wire disarm. PASS disarms an armed latch when a trusted final wire is safely below the proven limit; PASS does not disarm from a catalog-only limit. Six controls passed; only the named test failed.",
      "applied_evidence": "packages/plugin/src/hooks/magic-context/transform-postprocess-phase.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Treat unsupported message parts as fully countable in final-wire estimation.",
      "expected_red": "unsupported nontext parts never produce a trusted fit estimate",
      "captured_output": "FAIL unsupported nontext parts never produce a trusted fit estimate: trusted became true. PASS nonfinite system mass cannot be trusted even with known tool definitions; PASS unknown-model fit inflates raw mass instead of admitting a locally-fitting request. Only the named test failed.",
      "applied_evidence": "packages/plugin/src/hooks/magic-context/final-wire-token-estimate.ts: git diff --stat empty before mutation; 1 file changed, 2 insertions(+), 2 deletions(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Bypass the complete calibrated producer-prompt wall comparison.",
      "expected_red": "complete producer prompt uses producer calibration and refuses previously raw-fitting input",
      "captured_output": "FAIL complete producer prompt uses producer calibration and refuses previously raw-fitting input: refusal became null. PASS producer source below the margined input limit is admitted; remaining source-window controls passed. Only the named test failed.",
      "applied_evidence": "packages/plugin/src/hooks/magic-context/producer-window-guard.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Bypass the Pi calibrated fallback wall comparison with complete observations.",
      "expected_red": "Pi admits a complete calibrated fallback and refuses the locally fitting over-wall request",
      "captured_output": "FAIL Pi admits a complete calibrated fallback and refuses the locally fitting over-wall request: expected refusal no longer occurred. PASS Pi refuses incomplete fallback even when the byte proxy fits. Only the named test failed.",
      "applied_evidence": "packages/pi-plugin/src/pi-raw-fallback.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Fabricate small system/tool measurements when Pi fallback observations are absent.",
      "expected_red": "Pi refuses incomplete fallback even when the byte proxy fits",
      "captured_output": "FAIL Pi refuses incomplete fallback even when the byte proxy fits: missing observations were admitted. PASS Pi admits a complete calibrated fallback and refuses the locally fitting over-wall request. Only the named test failed.",
      "applied_evidence": "packages/pi-plugin/src/pi-raw-fallback.ts: git diff --stat empty before mutation; 1 file changed, 1 insertion(+) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    },
    {
      "control": "Bypass Rust complete producer admission before ScriptedProducer.start.",
      "expected_red": "historian::tests::calibrated_full_prompt_refuses_before_producer_start",
      "captured_output": "FAIL historian::tests::calibrated_full_prompt_refuses_before_producer_start: scripted start result available (the forbidden start was reached). The other 53 historian tests passed, including wired_historian_happy_path_sends_validates_and_publishes.",
      "applied_evidence": "crates/mc-module/src/historian.rs: git diff --stat empty before mutation; 1 file changed, 1 insertion(+), 1 deletion(-) while applied; empty after checkout and touch.",
      "outcome": "reddened"
    }
  ],
  "not_claimed": [
    "No defer-freeze neutralization against the strengthened priced host fixture was executed.",
    "These guard controls do not substitute for a complete scheduler-to-provider adversarial HTTP sequence on every harness."
  ]
}
